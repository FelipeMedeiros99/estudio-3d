services:
  catalogo-mateus:
    image: "node:22"
    build: .
    user: "0"
    volumes:
      - ./:/app
      - /app/node_modules
    ports: # use if it is necessary to expose the container to the host machine
      - "3001:3000"
    command: ["npm", "run", "dev"]
